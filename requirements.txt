# Core dependencies.
# NOTE: install PyTorch FIRST with the right build for your machine (see README),
#       then run:  pip install -r requirements.txt
pyannote.audio>=4.0
torch
torchaudio
soundfile>=0.12
numpy>=1.24
python-dotenv>=1.0

# Optional — only needed to diarize directly from a YouTube/media URL:
yt-dlp
